# freedesktop.org answers a runner with 418, so uchardet cannot fetch its
# tarball in CI. `origin` is the fallback for what the backup does not carry.
core.sources:download_urls=["https://c3i.jfrog.io/artifactory/conan-center-backup-sources/", "origin"]
