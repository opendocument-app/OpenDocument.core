android.useAndroidX=true
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=1g
org.gradle.configuration-cache=true

# AGP 9 brings its own kotlin, so there is no plugin and no version to pin here.
# It puts kotlin-stdlib on the AAR's POM, which OpenDocument.droid — kotlin
# itself, and the consumer this is built for — already depends on.
