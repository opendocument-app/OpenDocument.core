#!/usr/bin/env python3
"""Serves the annotation checks."""

import pathlib
import sys

HERE = pathlib.Path(__file__).resolve().parent
sys.path.insert(0, str(HERE.parent))

from serve import serve  # noqa: E402

serve(8733, HERE, ("tests.html",))
